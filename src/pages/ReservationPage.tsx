import { useMemo, useState } from "react";
import type { FormEvent } from "react";
import { Navigate, useLocation, useNavigate } from "react-router-dom";
import { hasReservationConflict, saveReservation } from "../data/rooms";
import type { PaymentMethod, PromotionCode, Reservation } from "../data/rooms";
import { getEffectiveRooms } from "../services/hotelService";
import { authRepository } from "../repositories/authRepository";
import "./ReservationPage.css";

const today = new Date().toISOString().slice(0, 10);
const promotions: Record<PromotionCode, { label: string; discount: number }> = {
  FINDE10: { label: "Fin de semana · 10%", discount: 10 },
  FAMILIA15: { label: "Experiencia familiar · 15%", discount: 15 },
  SUITE12: { label: "Suite Premium · 12%", discount: 12 },
};

function ReservationPage() {
  const navigate = useNavigate(); const location = useLocation(); const user = authRepository.getCurrentUser(); const rooms = getEffectiveRooms();
  const selectedRoomNumber = new URLSearchParams(location.search).get("room") || ""; const firstAvailable = rooms.find(r => r.status === "Disponible");
  const selectedRoom = rooms.find(r => r.number === selectedRoomNumber) || firstAvailable;
  const [roomNumber, setRoomNumber] = useState(selectedRoom?.number || ""); const [guestName, setGuestName] = useState(user?.name || ""); const [document, setDocument] = useState(user?.carnet || "");
  const [phone, setPhone] = useState(""); const [email, setEmail] = useState(""); const [checkIn, setCheckIn] = useState(""); const [checkOut, setCheckOut] = useState(""); const [guests, setGuests] = useState(1);
  const [paymentMethod, setPaymentMethod] = useState<PaymentMethod>("Efectivo"); const [paymentReference, setPaymentReference] = useState(""); const [promotionCode, setPromotionCode] = useState<"Ninguna" | PromotionCode>("Ninguna"); const [message, setMessage] = useState(""); const [confirmed, setConfirmed] = useState<Reservation | null>(null);
  const room = rooms.find(item => item.number === roomNumber);
  const nights = useMemo(() => { if (!checkIn || !checkOut) return 0; const start = new Date(`${checkIn}T00:00:00`).getTime(); const end = new Date(`${checkOut}T00:00:00`).getTime(); return end > start ? Math.ceil((end - start) / 86400000) : 0; }, [checkIn, checkOut]);
  const subtotal = room ? nights * room.price : 0; const promotion = promotionCode === "Ninguna" ? null : promotions[promotionCode]; const discount = promotion ? Math.round(subtotal * promotion.discount) / 100 : 0; const total = subtotal - discount;
  if (!user) return <Navigate to={`/login?redirect=${encodeURIComponent(location.pathname + location.search)}`} replace />;
  const handleRoomChange = (value: string) => { setRoomNumber(value); const r = rooms.find(x => x.number === value); if (r && guests > r.capacity) setGuests(r.capacity); setPromotionCode("Ninguna"); setMessage(""); };
  const handleSubmit = (event: FormEvent<HTMLFormElement>) => {
    event.preventDefault(); setMessage("");
    if (!room) return setMessage("Selecciona una habitación."); if (room.status !== "Disponible") return setMessage("La habitación seleccionada no está disponible.");
    if (!guestName.trim() || !document.trim() || !phone.trim() || !email.trim()) return setMessage("Completa todos los datos del huésped."); if (!checkIn || !checkOut || nights <= 0) return setMessage("Selecciona fechas válidas de entrada y salida.");
    if (checkIn < today) return setMessage("La fecha de entrada no puede ser anterior a hoy."); if (checkIn < room.availableFrom || checkOut > room.availableTo) return setMessage(`Esta habitación está disponible entre ${room.availableFrom} y ${room.availableTo}.`);
    if (guests < 1 || guests > room.capacity) return setMessage(`Esta habitación tiene capacidad máxima para ${room.capacity} huéspedes.`); if (hasReservationConflict(room.number, checkIn, checkOut)) return setMessage("La habitación ya tiene una reserva en esas fechas. Elige otras fechas o habitación.");
    if (paymentMethod === "Transferencia bancaria" && !paymentReference.trim()) return setMessage("Ingresa la referencia de la transferencia para continuar.");
    const paymentStatus = paymentMethod === "Tarjeta" ? "Pagado" : paymentMethod === "Transferencia bancaria" ? "Pendiente de verificación" : "Pendiente";
    const reservation: Reservation = { id: `RES-${Date.now()}`, userId: user.id, roomNumber: room.number, guestName: guestName.trim(), document: document.trim(), phone: phone.trim(), email: email.trim(), checkIn, checkOut, guests, subtotal, discount, promotionCode: promotionCode === "Ninguna" ? undefined : promotionCode, total, createdAt: new Date().toISOString(), status: "Activa", operationStatus: "Reservada", paymentMethod, paymentStatus, paymentReference: paymentReference.trim() || undefined };
    saveReservation(reservation); setConfirmed(reservation);
  };
  if (confirmed) return <main className="reservation-page"><nav className="reservation-navbar"><button onClick={() => navigate("/")}>Hotel Rolex</button><button onClick={() => navigate("/habitaciones")}>← Habitaciones</button></nav><section className="confirmation-card"><div className="confirmation-icon">✓</div><span>RESERVA CONFIRMADA</span><h1>¡Reserva realizada!</h1><p>Tu reserva fue registrada correctamente en este dispositivo.</p><div className="confirmation-details"><div><b>Código de reserva</b><span>{confirmed.id}</span></div><div><b>Habitación</b><span>{confirmed.roomNumber} · {room?.type}</span></div><div><b>Huésped</b><span>{confirmed.guestName}</span></div><div><b>Entrada</b><span>{confirmed.checkIn}</span></div><div><b>Salida</b><span>{confirmed.checkOut}</span></div><div><b>Huéspedes</b><span>{confirmed.guests}</span></div><div><b>Método de pago</b><span>{confirmed.paymentMethod}</span></div><div><b>Estado del pago</b><span>{confirmed.paymentStatus}</span></div>{confirmed.discount ? <div><b>Descuento</b><span>- ${confirmed.discount}</span></div> : null}<div className="confirmation-total"><b>Total</b><span>${confirmed.total}</span></div></div><div className="confirmation-actions"><button onClick={() => navigate(`/comprobante/${confirmed.id}`)}>Ver comprobante</button><button onClick={() => navigate("/perfil")}>Ver mi reserva</button><button className="outline" onClick={() => navigate("/habitaciones")}>Volver a habitaciones</button></div></section></main>;
  return <main className="reservation-page"><nav className="reservation-navbar"><button onClick={() => navigate("/")}>Hotel Rolex</button><button onClick={() => navigate("/habitaciones")}>← Habitaciones</button></nav><section className="reservation-layout"><div className="reservation-intro"><span>HOTEL ROLEX · RESERVAS</span><h1>Haz tu reserva.</h1><p>Completa tus datos, selecciona las fechas, aprovecha una promoción y elige cómo pagar.</p><div className="selected-room"><b>USUARIO REGISTRADO</b><strong>{user.name}</strong><span>Carnet: {user.carnet} · {user.role === "ADMIN" ? "Administrador" : "Usuario"}</span><small>✓ Tu cuenta está habilitada para realizar reservas</small></div>{room && <div className="selected-room"><b>HABITACIÓN SELECCIONADA</b><strong>{room.number} · {room.type}</strong><span>Hasta {room.capacity} huéspedes · ${room.price}/noche</span><small>✓ Baño privado · ✓ Wi-Fi · ✓ Atención hotelera</small></div>}<div className="reservation-steps"><div><b>01</b><span>Estancia</span></div><div><b>02</b><span>Huésped</span></div><div><b>03</b><span>Pago</span></div></div></div><form className="reservation-form" onSubmit={handleSubmit}><div className="form-heading"><span>01 · ESTANCIA</span><h2>Datos de la reserva</h2><p>Indica cuándo y dónde quieres hospedarte.</p></div><label>Habitación<select value={roomNumber} onChange={e => handleRoomChange(e.target.value)} required>{rooms.filter(r => r.status === "Disponible").map(r => <option key={r.number} value={r.number}>{r.number} · {r.type} · ${r.price}/noche</option>)}</select></label><div className="form-grid"><label>Fecha de entrada<input type="date" value={checkIn} min={today} onChange={e => { setCheckIn(e.target.value); if (checkOut && e.target.value >= checkOut) setCheckOut(""); }} required /></label><label>Fecha de salida<input type="date" value={checkOut} min={checkIn ? new Date(new Date(`${checkIn}T00:00:00`).getTime() + 86400000).toISOString().slice(0, 10) : today} onChange={e => setCheckOut(e.target.value)} required /></label></div><label>Número de huéspedes<input type="number" min="1" max={room?.capacity || 6} value={guests} onChange={e => setGuests(Number(e.target.value))} required /></label><div className="form-heading second"><span>02 · HUÉSPED</span><h2>Datos del huésped</h2><p>Usaremos estos datos para identificar la reserva.</p></div><label>Nombre completo<input value={guestName} onChange={e => setGuestName(e.target.value)} required /></label><div className="form-grid"><label>Documento<input value={document} onChange={e => setDocument(e.target.value)} required /></label><label>Teléfono<input type="tel" value={phone} onChange={e => setPhone(e.target.value)} required /></label></div><label>Correo electrónico<input type="email" value={email} onChange={e => setEmail(e.target.value)} required /></label><div className="form-heading second"><span>03 · OFERTA</span><h2>Promoción</h2><p>Aplica una promoción disponible para tu tipo de habitación.</p></div><label>Código de promoción<select value={promotionCode} onChange={e => setPromotionCode(e.target.value as "Ninguna" | PromotionCode)}><option value="Ninguna">Sin promoción</option>{room?.type === "Estándar" && <option value="FINDE10">FINDE10 · 10% de descuento</option>}{room?.type === "Familiar" && <option value="FAMILIA15">FAMILIA15 · 15% de descuento</option>}{room?.type === "Suite Premium" && <option value="SUITE12">SUITE12 · 12% de descuento</option>}</select></label><div className="form-heading second"><span>04 · PAGO</span><h2>¿Cómo deseas pagar?</h2><p>Elige una opción de pago para tu reserva.</p></div><div className="payment-options"><label className={`payment-option ${paymentMethod === "Tarjeta" ? "selected" : ""}`}><input type="radio" name="payment" checked={paymentMethod === "Tarjeta"} onChange={() => { setPaymentMethod("Tarjeta"); setPaymentReference(""); }} /><span className="payment-icon">▣</span><span><strong>Tarjeta</strong><small>Pago simulado para demostración.</small></span></label><label className={`payment-option ${paymentMethod === "Transferencia bancaria" ? "selected" : ""}`}><input type="radio" name="payment" checked={paymentMethod === "Transferencia bancaria"} onChange={() => setPaymentMethod("Transferencia bancaria")} /><span className="payment-icon">⇄</span><span><strong>Transferencia</strong><small>Queda pendiente de verificación.</small></span></label><label className={`payment-option ${paymentMethod === "Efectivo" ? "selected" : ""}`}><input type="radio" name="payment" checked={paymentMethod === "Efectivo"} onChange={() => { setPaymentMethod("Efectivo"); setPaymentReference(""); }} /><span className="payment-icon">Bs</span><span><strong>Efectivo</strong><small>Pago en recepción.</small></span></label></div>{paymentMethod === "Tarjeta" && <div className="payment-info"><strong>Pago con tarjeta</strong><p>En esta versión es demostrativo. No se guardan datos de tarjeta ni códigos de seguridad.</p></div>}{paymentMethod === "Transferencia bancaria" && <div className="payment-info"><strong>Datos para transferencia</strong><p>Banco: Hotel Rolex · Cuenta: 0000000000 · Titular: Hotel Rolex</p><label>Referencia<input value={paymentReference} onChange={e => setPaymentReference(e.target.value)} placeholder="Ej. TRF-123456" required /></label><span>Estado: Pendiente de verificación por administración.</span></div>}{paymentMethod === "Efectivo" && <div className="payment-info"><strong>Pago en efectivo</strong><p>La reserva se confirma y el pago queda pendiente hasta llegar a recepción.</p><span>Estado: Pendiente</span></div>}{nights > 0 && <div className="price-box"><div><span>Estancia</span><strong>{nights} noche(s) × ${room?.price || 0}</strong></div>{discount > 0 && <div><span>Descuento</span><strong>- ${discount}</strong></div>}<div><span>Total</span><strong>${total}</strong></div></div>}{message && <div className="reservation-error">⚠ {message}</div>}<button className="confirm-button" type="submit">Confirmar reserva y pago <span>→</span></button><p className="saved-note">Los pagos con tarjeta son demostrativos y las transferencias requieren verificación administrativa.</p></form></section></main>;
}
export default ReservationPage;
